#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int v, t;
    while(cin >> v >> t) {
        cout << v * t * 2 << endl;
    }

    return 0;
}
